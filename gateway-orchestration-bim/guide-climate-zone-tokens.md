---
schema: foundry-doc-v1
title: "Working with climate zone tokens"
slug: guide-climate-zone-tokens
type: guide
status: active
bcsc_class: public-disclosure-safe
last_edited: 2026-05-25
editor: pointsav-engineering
cites: [ifc-4-3, ids-1-0, ashrae-90-1, nbc-2020]
---

# Working with climate zone tokens

This guide explains how climate zone performance data is structured in the BIM token vault and how it is consumed by the BIM orchestration engine.

## What climate zone data is

Climate zone data is performance specification data embedded in BIM tokens. It is not a user-selectable dropdown — it is reference data showing the performance requirements that apply in each registered climate zone for a given element type.

The data lives in `tokens/bim/climate-zones.dtcg.json` in the BIM token vault.

## File location

```
<token-vault>/
└── tokens/
    └── bim/
        └── climate-zones.dtcg.json
```

Each entry in the file is a performance parameter for one element type in one climate zone:

```json
{
  "$type": "bim-climate-zone",
  "zone_id": "ASHRAE-5C",
  "ifc_class": "IfcWall",
  "parameter": "max_u_value",
  "required_value": 0.104,
  "unit": "Btu/h·ft²·°F",
  "source_standard": "ASHRAE 90.1-2022 Table 5.5-5",
  "$value": "climate-zone-spec"
}
```

## Token resolution workflow

1. **Site assignment.** The project site is linked to an `IfcSpatialZone` (PredefinedType: `USERDEFINED`, ObjectType: `CLIMATE_ZONE`) with the applicable zone identifier (e.g., `ASHRAE-5C`).

2. **Token lookup.** When an element token is placed, the token resolver reads `climate-zones.dtcg.json` and finds all rows where `ifc_class` matches the placed element type and `zone_id` matches the project's assigned zone.

3. **Performance binding.** The matched rows are bound to the element as required property values. These appear in the element's Regulation tab as Climate Zone rows.

4. **IFC projection.** The resolved climate zone requirements are projected into the IFC-SPF archive as `IfcConstraint` records linked to the element via `IfcRelAssociatesConstraint`.

## Validation

Use `ifctester` with an IDS 1.0 contract generated from the climate zone token data to verify that every element satisfies the climate zone requirements for its type:

```bash
# From the app-orchestration-bim project root
ifctester --ids climate-zones-bc-5c.ids model.ifc --report json
```

The IDS contract file for a climate zone is generated by the token vault tooling (planned for v0.0.3). At v0.0.1, the IDS contract must be authored manually from the `climate-zones.dtcg.json` parameter rows.

## Adding a new climate zone

To add performance data for a new climate zone:

1. Open `tokens/bim/climate-zones.dtcg.json` in the token vault.
2. Add rows for each element type and performance parameter in the new zone, following the existing row structure.
3. Commit to the token vault repository with message: `feat(climate-zones): add <zone-id> performance parameters`.
4. Update the BIM Token Catalog by restarting the BIM orchestration engine (the vault is read at startup).
5. If the zone requires an IDS constraint file, author the IDS file and add it to `climate/<zone-id>/` in the vault.

## Registered climate zones at v0.0.1

| Zone ID | Description | Element types covered |
|---|---|---|
| ASHRAE-5C | Marine (Pacific Northwest, BC coast) | IfcWall, IfcSlab, IfcWindow |
| ASHRAE-6 | Cold | IfcWall, IfcSlab, IfcWindow |
| ASHRAE-7A | Very cold | IfcWall, IfcSlab |
| NBC-Zone-6 | Canadian cold climate | IfcWall, IfcSlab, IfcRoof |

Additional zones are planned for v0.0.3 as part of the BC RS-1 regulatory overlay set.
